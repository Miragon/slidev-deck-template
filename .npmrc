; Keep every new dependency pinned to an exact version.
; Ranges (^, ~, latest, *) reintroduce the supply-chain risk that
; exact pinning removes: npm install could pull an unreviewed build.
save-exact=true
